# AGENTS.md

このリポジトリは、mdFOAM/OpenFOAM形式のMDシミュレーション結果を解析するPythonデスクトップアプリです。

重要: ParaView と pvpython には依存しません。解析は可能な限りOpenFOAM ASCIIファイルをPythonで直接読み取って行います。

## 目的

100ケース程度の計算結果から、密度がしきい値以上の領域について以下を自動計算し、GUI上で確認できるようにします。

- 体積
- 等価半径 `R_eq = (3V / (4*pi))^(1/3)`
- 接触角
- 接触半径
- 蒸発完了時刻
- ケースごとの結果表
- 選択ケースの体積・等価半径・接触角・接触半径グラフ
- 全ケースの蒸発完了時刻グラフ
- CSV/PNG出力

## リポジトリ状態

このリポジトリにはサンプル計算データを含めません。ローカル検証用に `run001_x001/` のようなケースを置くことはありますが、Git管理対象にしないでください。

削除済みのサンプルデータには、`initial/`, `initial_a/`, `main/`, `AllDisMpi_*`, `batch_*.out`, `batch_status.txt`, `case_info.txt`, `log.mapFields` などがありました。今後も大きな計算結果やケースデータはリポジトリに入れないでください。

## 想定する入力構造

複数ケース:

```text
parent/
  case001/
    main/
      constant/polyMesh/
      <time>/
        rhoM_water
        ...
  case002/
    main/
      ...
```

単一ケース:

```text
case001/
  main/
    constant/polyMesh/
    <time>/
      rhoM_water
      ...
```

ケース検出は、選択フォルダ自身が `main` を持つ場合は単一ケース、選択フォルダ直下の子フォルダが `main` を持つ場合は複数ケースとして扱います。

解析時は `main/<time>/<density field>` の再構成済み時刻ディレクトリを優先して読みます。再構成済みデータが無い場合は `main/processor*/<time>/<density field>` を合算します。

## データ形式と解析前提

- 既定の密度フィールドは `rhoM_water`
- 密度フィールドはOpenFOAM ASCII形式の `volScalarField` を想定
- 密度はセル中心データとして扱う
- `internalField nonuniform List<scalar>` にセル数ぶんの値が入っている想定
- GUIでは `rhoM_*` / `rhoN_*` フィールドを選択可能
- `main/0` のような数値時刻ディレクトリでも、選択密度フィールドが無ければ解析対象時刻には含めない
- 接触角・接触半径は、選択密度フィールドでしきい値以上のセル中心点を液滴点群として計算する
- xy方向周期境界条件の液滴を想定し、接触角計算では既定でxy座標を周期補正してから球フィットする

## セル体積とセル中心

`V` のようなセル体積フィールドが無いケースを主対象にしています。

そのため、Pythonで `constant/polyMesh` の以下を直接読んでセル体積とセル中心を計算します。

- `points`
- `faces`
- `owner`
- `neighbour`

セル体積は一定と仮定しないでください。過去のサンプルではセル体積が2種類ありました。

メッシュから体積計算できない場合だけ、GUI入力のセル体積または `dx, dy, dz` を使います。このfallbackではセル中心が無いため、接触角・接触半径は空欄になります。

## 接触角・接触半径

接触角計算は、過去に使っていた「ParaViewでContourを取った点群に `Sesshoku6.py` を適用する」手順に近づけます。
ParaView/pvpythonには依存せず、Pythonで密度しきい値をまたぐ隣接セル中心間を線形補間して等値面点群を作り、その点群に `Sesshoku6.py` と同じ球フィット手順を適用します。

- 液滴点群: 密度しきい値の等値面点群
- 基板高さ: `z_min + 0.05 * z_height` 以下の点の平均z
- fit範囲: `z_min + fit_lower * z_height` から `z_min + fit_upper * z_height`
- 既定fit範囲: `fit_lower=0.5`, `fit_upper=1.0`
- 球フィット: `x^2 + y^2 + z^2 + Dx + Ey + Fz + G = 0` の最小二乗
- 接触角: `theta = acos((z_base - zc) / R)` をdegreeで出力
- 接触半径: `r_contact = R * sin(theta)`
- fit点が4点未満、`z_height == 0`、`R < 1e-12` の場合は空欄
- xy周期補正は既定ON。メッシュのx/y範囲から周期長を推定し、円周平均を中心にunwrapする。

## 解析設定の既定値

- 密度しきい値: `500`
- 0判定許容値: `0`
- 連続ゼロ数: `3`
- 接触角fit下限: `0.5`
- 接触角fit上限: `1.0`
- 平均接触角の対象範囲: `100%`
- xy周期補正: 有効
- 蒸発完了時刻: 体積が0相当の状態が連続ゼロ数ぶん続いたとき、その連続区間の最初の時刻

例:

```text
1.17e-08: 非ゼロ
1.18e-08: 非ゼロ
1.19e-08: 0
1.20e-08: 0
1.21e-08: 0
```

この場合の蒸発完了時刻は `1.19e-08` です。

## 過去サンプルでの検証値

サンプルデータは削除済みですが、開発時に使った `rhoM_water >= 500` の検証では以下を確認していました。

- 時刻数: `127`
- 最大体積: 約 `3.97004541958126e-26`
- 最終体積: `0`
- 蒸発完了時刻: `1.19e-08`
- セル数: `3825`
- セル体積は2種類
- `1e-10` 付近の接触角: 約 `81.10057426317968 deg`
- `1e-10` 付近の接触半径: 約 `2.8254863800372293e-09 m`
- 液滴セルやfit点が少ない後半時刻は接触角・接触半径が空欄になる

これらは回帰確認用の参考値です。現在のリポジトリ内で再実行するには、同等の外部ケースデータを別途用意する必要があります。

## 実装構成

起動:

```powershell
python app.py
```

Windowsで右クリック起動したい場合:

```powershell
.\run_app.bat
```

依存関係:

```powershell
python -m pip install -r requirements.txt
```

主なファイル:

- `app.py`: 起動入口
- `run_app.bat`: この環境のMiniconda Pythonを明示して起動するWindows用ランチャー
- `Sesshoku6.py`: 接触角計算の元スクリプト。アプリ本体はこの手順を移植して使う
- `src/mdfoam_analyzer/openfoam.py`: OpenFOAM ASCIIファイル読み取り、メッシュ体積・セル中心計算
- `src/mdfoam_analyzer/analysis.py`: ケース検出、体積・等価半径・蒸発完了時刻・接触角・接触半径計算、CSV出力
- `src/mdfoam_analyzer/gui.py`: PySide6 GUI、表、グラフ、Run/Stop、CSV/PNG出力

## GUI仕様

- 日本語UI
- 親フォルダ選択
- ケース一覧
- 密度フィールド選択
- 密度しきい値、0判定許容値、連続ゼロ数、セル体積/dx/dy/dz入力
- 接触角fit下限・上限、xy周期補正ON/OFF
- バックグラウンド解析とStop
- 結果テーブルには初期接触角、最終有効接触角、初期接触半径、最終有効接触半径も表示
- 結果テーブルには、ケース内の時系列先頭から指定%までの有効な接触角で求めた平均接触角も表示
- 表は列ヘッダークリックで列選択可能
- `Ctrl+C` でExcel貼り付け用TSVコピー可能
- 体積・等価半径・接触角・接触半径グラフは折れ線ではなく点表示
- 全ケースの蒸発完了時刻は棒グラフ
- 時系列CSVには `contact_angle_deg`, `contact_radius`, `contact_fit_point_count` を出力

## 開発時の注意

- ParaView/pvpythonへの依存は追加しないでください。
- OpenFOAM ASCIIファイルを直接読む方針を維持してください。
- 既定フィールドは `rhoM_water` のままにしてください。
- 蒸発完了時刻は「連続ゼロ区間の最後」ではなく「最初」の時刻です。
- セル体積は一定と仮定しないでください。
- 接触角計算は等値面点群を入力にし、`Sesshoku6.py` の球フィット式・基板高さ・接触半径定義に従ってください。
- xy方向周期境界をまたぐ液滴で接触角が崩れないよう、周期補正の既定ONを維持してください。
- グラフの体積・等価半径・接触角・接触半径は点表示がユーザー希望です。

## 今後の改良候補

- 複数ケースが100個近くある親フォルダでの実測性能確認
- 解析済み結果のキャッシュ
- ケース名からパラメータを抽出して表やグラフ軸に使う機能
- 時系列CSVから再読み込みして再プロットする機能
- しきい値を複数指定して比較する機能
- 体積単位や半径単位をGUIで切り替える機能
- エラーケースだけをフィルタ表示する機能
- グラフ上で選択ケースを複数重ねる機能
- 接触角fitの断面図をGUI上に表示する機能
